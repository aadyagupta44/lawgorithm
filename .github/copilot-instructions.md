# Copilot Instructions for Capstone RAG

## Overview
This document provides guidance for AI coding agents working within the Capstone RAG codebase. Understanding the architecture, workflows, and conventions is crucial for effective contributions.

## Architecture
- **Major Components**: The codebase is structured around several key components, including [Component A], [Component B], and [Component C]. Each component serves a specific purpose and interacts with others through defined interfaces.
- **Service Boundaries**: Clearly defined service boundaries help maintain separation of concerns. For example, [Service A] handles [specific functionality], while [Service B] manages [another functionality].
- **Data Flows**: Data flows between components are managed through [specific protocols or patterns]. Understanding these flows is essential for debugging and feature development.

## Developer Workflows
- **Building the Project**: Use the command `build-command` to compile the project. Ensure all dependencies are installed beforehand.
- **Running Tests**: Execute `test-command` to run the test suite. Tests are located in the `tests/` directory and follow the naming convention [specific naming convention].
- **Debugging**: Utilize [specific debugging tools or techniques] to troubleshoot issues effectively. Refer to [specific files or configurations] for setup.

## Project-Specific Conventions
- **Code Style**: Follow the [specific style guide] for consistent code formatting. Use [specific tools] for linting and formatting.
- **Naming Conventions**: Variables and functions should be named according to [specific conventions]. For example, [example of naming convention].

## Integration Points
- **External Dependencies**: The project relies on [list of external libraries or services]. Ensure these are properly configured in [specific configuration files].
- **Cross-Component Communication**: Components communicate via [specific methods or protocols]. Understanding these interactions is key to maintaining the integrity of the system.

## Examples
- **Example of a Component Interaction**: Describe a specific interaction between two components, including code snippets if applicable.
- **Common Patterns**: Document any common patterns observed in the codebase, such as [specific design patterns].

## Conclusion
This document should serve as a foundational guide for AI agents working within the Capstone RAG codebase. For further questions, refer to [specific resources or contacts].